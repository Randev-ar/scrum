name: 🔁 SCRUM PROYECT

on:
  issues:
    types: [opened]

jobs:
  scrum:
    uses: Randev-ar/gh-workflows/.github/workflows/scrum.yml@main
    with:
      project-url: ${{ secrets.TEAM_PROJECT_URL }}
      github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
