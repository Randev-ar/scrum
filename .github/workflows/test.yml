name: 🔁 SCRUM PROYECT

on:
  issues:
    types: [opened]

jobs:
  scrum:
    uses: https://github.com/Randev-ar/gh-workflows/blob/main/.github/workflows/scrum.yml
    with:
      project-url: ${{ secrets.TEAM_PROJECT_URL }}
      github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
