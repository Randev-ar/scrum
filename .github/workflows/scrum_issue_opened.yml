name: 🔁 SCRUM PROJECT

on:
  issues:
    types: [opened]

jobs:
  addIssueToProject:
    name: 📝 Adding Issue to Project
    uses: ./.github/workflows/add_issue_to_project.yml
    secrets:
      TEAM_PROJECT_URL: ${{ secrets.TEAM_PROJECT_URL }}
      ADD_TO_PROJECT_PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}

  epic:
    name: 🔁 SCRUM PROJECT - New Epic
    uses: Randev-ar/gh-workflows/.github/workflows/scrum_new_epic.yml@main
    secrets:
      TEAM_PROJECT_URL: ${{ secrets.TEAM_PROJECT_URL }}
      ADD_TO_PROJECT_PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}
