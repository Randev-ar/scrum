name: 🔁 SCRUM PROJECT

on:
  issues:
    types: [opened]

jobs:
  scrum:
    name: 🔁 SCRUM PROJECT - ISSUE OPENED
    uses: Randev-ar/gh-workflows/.github/workflows/scrum.yml@main
    secrets:
      TEAM_PROJECT_URL: ${{ secrets.TEAM_PROJECT_URL }}
      ADD_TO_PROJECT_PAT: ${{ secrets.ADD_TO_PROJECT_PAT }}
